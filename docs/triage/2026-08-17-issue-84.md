# Triage: issue #84 — A&O Shearman FinReg AML-package page changed (manual review)

- **Issue:** https://github.com/frankdevlabs/aml-package-2024/issues/84
- **Source:** A&O Shearman — finreg AML package tracker (`T2-01`) — https://finreg.aoshearman.com/Package-of-EU-Anti-Money-Laundering-Legislation-P
- **Detected:** 2026-08-17T14:09:26Z
- **Type / playbook:** stakeholder / stakeholder-social

## What was flagged
The normalized-body hash of A&O Shearman's dedicated EU-AML-package tracker page changed
from its stable baseline. The article list is client-rendered (JS), so the daily tracker
could not see what was added server-side; the month index now runs through **August 2026**.
Manual review of the live page was requested.

## Assessment
**No substantive change confirmed — recommend close as not-relevant; no repo edit.**

- WebFetch of the live URL returned only the static shell (the "…Legislation Published" June 2024
  overview), with 2026 article rows absent — confirming the tracker's note that rows load via JS.
- A headless/JS render was attempted (`~/law-tracker/lib/fetch_blocked_doc.py`); it succeeded only at
  the **curl tier** (57 KB, HTTP 200 — this is not a WAF block, so no browser tier engaged). The
  returned HTML contains the month-selector `<option>` list (options through `2026.08` = August 2026)
  and boilerplate, but **no dated article entries** — grepping for article titles/dates/`publishedDate`
  keys yields only the dropdown options and page chrome.
- No JS-rendering browser is available in this environment to extract the article rows, so the
  substance of the hash change **cannot be confirmed server-side**.
- Most likely cause: the month index rolled over to a new **August 2026** bucket (routine, non-substantive).
- A&O Shearman is a **secondary aggregator/tracker**, already recorded in
  [`docs/stakeholders.md`](../stakeholders.md) as "package tracker confirming dates and staggered
  application." An unverifiable hash change on a client-rendered aggregator surfaces **no new law-firm
  position** and **no primary institutional document** (no AMLA consultation / draft RTS-ITS-GL / final
  instrument / transposition step) that would warrant upgrading to the `institutional` /
  `amla-instrument` playbook.

## Plan
1. **No content edit.** Neither [`docs/stakeholders.md`](../stakeholders.md) (already lists A&O Shearman)
   nor [`docs/amla-pipeline.md`](../amla-pipeline.md) requires a change: nothing substantive or verifiable
   was surfaced. Editing on an unreadable hash delta would risk asserting content not confirmed against
   the source.
2. **Re-baseline handled by the tracker.** The daily cron re-bases the page hash on its next run; no
   manual state edit (`data/tracker-state.yaml` is auto-maintained — do not hand-edit).
3. **Close the issue as not-relevant** once this plan merges. If a future run surfaces an actual dated
   entry citing a **primary** AMLA/transposition development, that will re-flag and can be triaged on its
   own merits (upgrading to the matching institutional/amla-instrument playbook per the source it names).

## Verification
- Link check: `python3 .claude/skills/transcribe-council-extract/linkcheck.py .` → must end "0 broken".
- Re-open the live page in a JS-capable browser if manual confirmation is desired; if a new substantive
  dated entry is found, reopen/replace with a `stakeholder-social` content edit to `docs/stakeholders.md`.
- Close #84 as not-relevant (no repo content change).
