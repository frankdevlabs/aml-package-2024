# Triage: issue #43 — DNB AML/CFT hub: new AMLA news items (IRAP; eligibility reporting)

- **Issue:** https://github.com/frankdevlabs/aml-package-2024/issues/43
- **Source:** DNB — Europees AML/CFT-kader hub (`T1-08`) — https://www.dnb.nl/voor-de-sector/open-boek-toezicht/wet-regelgeving/europees-amlcft-kader/
- **Detected:** 2026-07-08
- **Type / playbook:** institutional / institutional

> Note: the resolver tagged `playbook: council-text`, which is wrong here — there is no Council ST
> compromise text. This is a national-supervisor (DNB) guidance change; the `type` is `institutional`
> and `affects` = `docs/netherlands.md`, `STATUS.md`. Ran the `institutional` playbook.

## What was flagged

The DNB "Europees AML/CFT-kader" hub HTML changed since the 2026-06-25 baseline. Two AMLA-related
"Nieuwsberichten" are now teased in the hub sidebar:

1. **"Wijzigingen in IRAP door komst AMLA: bereid u voor op 2026 en 2027"**
   (`/nieuws-voor-de-sector/toezicht-2025/q4/wijzigingen-in-irap-door-komst-amla-bereid-u-voor-op-2026-en-2027/`)
2. **"AMLA eligibility reporting package: geen actie nodig voor instellingen onder DNB-toezicht"**
   (`/nieuws-voor-de-sector/toezicht-2026/q2/amla-eligibility-reporting-package-geen-actie-nodig-voor-instellingen-onder-dnb-toezicht/`)

Fetched the hub and the second item via the WAF workaround (Akamai 403's plain curl/WebFetch).

## Assessment

**In scope** — DNB national-supervisor guidance on AMLA onboarding; not a `keep_apart` sibling.

- **Item 1 (IRAP)** is **already recorded** in `docs/netherlands.md` §e (line 77, same `toezicht-2025/q4`
  URL). No new fact — the tracker re-surfaced it because both items now sit in the hub sidebar. Noise.
- **Item 2 (eligibility reporting package)** is **new and substantive.** Confirmed from the article
  (published **13 May 2026**): *"AMLA heeft op 12 mei het zogeheten eligibility reporting package
  gepubliceerd voor het verzamelen van informatie om te bepalen welke instellingen onder direct
  AMLA-toezicht komen te staan."* DNB's guidance: institutions under DNB supervision **need take no
  action** — DNB has already requested the relevant data via the **IRAP** for most institutions and
  will approach the rest directly; the AMLA package need not be independently completed or filed with DNB.

This is the NL-supervisor counterpart to AMLA's 12 May 2026 eligibility-reporting package — a concrete
step in AMLA's direct-supervision **selection** process (the ~40 entities picked in 2027; supervision
from 2028). `STATUS.md` already reflects the AMLA eligibility-reporting workstream in the AMLA row
(webinar materials, 10 Jun 2026); adding a DNB "no action needed" operational note to the one-screen
snapshot would over-clutter it — record it in `docs/netherlands.md` only.

## Plan

1. `docs/netherlands.md` §e — extend the DNB paragraph with a sourced sentence on the **eligibility
   reporting package** (AMLA 12 May 2026) and DNB's "geen actie nodig / already via IRAP" guidance,
   linking the DNB news item.
2. `STATUS.md` — **no change.** The AMLA eligibility-reporting selection workstream is already in the
   AMLA row; the DNB operational note is too granular for the snapshot.
3. Close #43 once merged (Item 1 already covered; Item 2 integrated).

## Verification

- `python3 .claude/skills/transcribe-council-extract/linkcheck.py .` → must end "0 broken".
- Read `docs/netherlands.md` §e — confirm the new eligibility-reporting sentence is present and sourced.
- `gh issue close 43` on merge.
