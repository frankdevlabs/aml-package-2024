# Triage: issue #95 — NVB sitemap restructured (/nieuws/ → /nieuws-en-verhalen/), hash-basis stale

- **Issue:** https://github.com/frankdevlabs/aml-package-2024/issues/95
- **Source:** Nederlandse Vereniging van Banken (NVB) — AML/UBO positions (via sitemap) (`T2-03`) — https://www.nvb.nl/sitemap.xml
- **Detected:** 2026-09-24T14:06:28Z
- **Type / playbook:** stakeholder / stakeholder-social (handled as source-config, see Assessment)

## What was flagged
The NVB website was restructured. The watched sitemap no longer serves news under `/nieuws/`;
matching articles now live under `/nieuws-en-verhalen/` and position papers under
`/position-papers-en-consultaties/`. The filtered-URL count (`witwas|aml|ubo|wwft`) collapsed from
the **43 matching** entries documented in `tracker.yaml` as of 2026-07-21 to a handful now. The
tracker flags this as a structural hash-basis change (like the 2026-07-08 restructure), not 37
deleted articles.

Live source confirms it (fetched 2026-09-24): the sitemap resolves, `/nieuws/` paths are gone, and
matching `<loc>` URLs now sit under `/nieuws-en-verhalen/`, `/position-papers-en-consultaties/` and
`/themas-en-standpunten/`. Matching entries include known NVB positions (financieel-economische
criminaliteit; inbreng commissiedebat witwassen; reactie op consultatie implementatiewet Wwft;
anti-witwasaanpak kabinet), all consistent with the substance already recorded in
`docs/stakeholders.md` / `docs/netherlands.md` / `docs/fault-lines.md`.

## Assessment
**In scope (this is the AML package's NL industry watch), but not a content change.** The flagged
event is a change to the *source structure*, not a new NVB position. The nominal `stakeholder-social`
playbook does not apply to the substance — this is a `source-health`-shaped config fix: the T2-03
filter path and hash-basis note in `tracker.yaml` are stale and must be corrected so future diffs are
meaningful and the daily hash stops mis-flagging.

- CLAUDE.md carries **no** NVB / sitemap reference (the issue's "tracker.yaml / CLAUDE.md" was
  slightly off) — the stale note lives only in `tracker.yaml` T2-03. No CLAUDE.md edit needed.
- No new substantive NVB position surfaced: the matching URLs map to positions already covered.
  The `nvb-reactie-op-consultatie-van-de-implementatiewet` URL (NL Wwft transposition) is the one
  to keep an eye on — left as a future-watch, not integrated here (no confirmed new content/date).
- `affects` docs (`docs/stakeholders.md`, `docs/netherlands.md`, `docs/fault-lines.md`) need **no**
  edit — their NVB coverage stands.

## Plan
1. Update `tracker.yaml` T2-03 `notes`:
   - Replace the `/nieuws/` filter-path guidance with `/nieuws-en-verhalen/` +
     `/position-papers-en-consultaties/`.
   - Refresh the hash-basis line: 43 matching (2026-07-21) → ~6 matching (2026-09-24); state the
     2026-09-24 drop is structural (sitemap restructure), not deleted articles.
   - Record the 2026-09-24 restructure alongside the existing 2026-07-08 history note.
2. No edits to `docs/stakeholders.md`, `docs/netherlands.md`, `docs/fault-lines.md` — coverage stands.

## Verification
- `python3 .claude/skills/transcribe-council-extract/linkcheck.py .` ends "0 broken".
- Re-read `tracker.yaml` T2-03: filter path names the two new sections; hash-basis line dated
  2026-09-24; history notes both restructures.
- Close #95 once merged (config corrected; no content owed).
