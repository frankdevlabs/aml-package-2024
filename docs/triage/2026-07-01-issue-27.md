# Triage: issue #27 — AMLA regulatory-instruments index lists three untracked draft instruments

- **Issue:** https://github.com/frankdevlabs/aml-package-2024/issues/27
- **Source:** AMLA — Regulatory instruments tracker (`T1-02`) — https://www.amla.europa.eu/policy/regulatory-instruments_en
- **Detected:** 2026-07-01
- **Type / playbook:** institutional / institutional

## What was flagged

The daily tracker saw the normalized HTML body of AMLA's **regulatory-instruments index** change since the
2026-06-30 run (hash stable on immediate re-fetch → real change, not a rotating widget). The page's own
"Last update" still reads **3 June 2026**, so no visible date bump — consistent with an in-table content edit.
Manual review requested to identify the delta.

## Assessment

**Substantive and in-scope.** Fetched the index (raw HTML via the WAF fetcher, plus the summariser) and
cross-checked every row against what the repo records. The **"Instruments under consultation / consultations
closed"** table now lists **12** instruments; the repo's pipeline coverage (`docs/amla-pipeline.md`) tracks
only **7**. Every status the repo already knows matches (group-wide RTS *closed*; BWRA + ongoing-monitoring GL
*open*; CDD / business-relationships / pecuniary-sanctions / ITS-15(3) *closed*; risk-profile 40(2) and
selection 12(7) *final report published*). The material delta is **three draft instruments that appear only on
this index — all marked "Consultation closed" — and that are absent from the public-consultations hub and from
the entire repo:**

| Type | Legal basis | Mandate | Index status |
|---|---|---|---|
| ITS | AMLR 81(1); AMLAR 41(2) | FIU and AMLA reporting to the EPPO | Consultation closed |
| ITS | AMLD6 31(2) | FIU-to-FIU exchanges | Consultation closed |
| RTS | AMLD6 46(4) | Home-Host Supervisory Cooperation | Consultation closed |

- **In-scope:** legal bases are AMLR (Reg 2024/1624), AMLA Reg (2024/1620) and AMLD6 (Dir **2024/1640**) — the
  package this repo tracks. None is the `keep_apart` criminal-law "6AMLD" (Dir 2018/1673). Confirmed with
  `grep`: none of "EPPO", "FIU-to-FIU", "Home-Host", "81(1)", "31(2)", "46(4)" appears anywhere in the repo.
- **Whether "new" or "long-present":** the prior HTML snapshot is not retained (the cron stores hashes only),
  so the exact byte-level change cannot be pinned. But these three are the only rows that differ from the
  repo's knowledge and are the plausible trigger; either way the integration is identical — capture them.
- **Retrieval limit (metadata-only):** these three are **not** on AMLA's
  [public-consultations hub](https://www.amla.europa.eu/policy/public-consultations_en) (re-checked 1 Jul 2026 —
  still the same 7), so there is **no consultation-page link, no open/close dates, and no draft PDF** reachable
  from the index. We can confirm their *existence + legal basis + closed status*, not their operative text. So
  this is a **capture-metadata-now, retrieve-text-later** case — no `extracts/amla/` transcription and no
  `data/documents.yaml` document entry yet (there is no PDF to register).

## Plan

Applied on branch `triage/2026-07-01-issue-27` (`--fix`):

1. **`docs/amla-pipeline.md`** — leave the 7-row hub-verified consultation status table unchanged (it is
   deep-linked to `extracts/amla/*` files that do not exist for these three). Add a new subsection
   **"Additional index-listed instruments"** with the three-row table above, the 1 Jul 2026 index-check note,
   and an explicit *pending* marker (papers/dates/text not yet retrieved).
2. **`TIMELINE.md`** — add a `2026-07-01` row recording that the regulatory-instruments index surfaced the
   three additional draft instruments (closed status), not on the consultations hub.
3. **`STATUS.md`** — add one **"Next milestones to watch"** line to retrieve the consultation papers / draft
   text for the three index-listed instruments (the follow-up trigger). `As of` is already 1 Jul 2026.

No document registration, no extract transcription — deferred until a consultation page or draft PDF surfaces.

## Verification

- Sources read: regulatory-instruments index (raw HTML, 12 rows confirmed verbatim) + public-consultations hub
  (7 rows, none of the three) — both fetched 1 Jul 2026.
- Link check: `python3 .claude/skills/transcribe-council-extract/linkcheck.py .` → must end **0 broken**.
- Follow-up: when a consultation page or draft PDF appears for any of the three, run the `amla-instrument`
  workflow — register the document, build the `extracts/amla/` slice, and clear the pending note.
- Close: `gh issue close 27 --comment "Captured three index-only AMLA draft instruments (EPPO-reporting ITS 81(1)/41(2); FIU-to-FIU ITS 31(2); Home-Host RTS 46(4)) in amla-pipeline.md / TIMELINE.md / STATUS.md; operative text pending. See PR."` (on merge).
