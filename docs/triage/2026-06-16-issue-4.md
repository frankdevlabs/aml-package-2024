# Triage: issue #4 — AMLA regulatory-instruments index changed (manual review)

- **Issue:** https://github.com/frankdevlabs/aml-package-2024/issues/4
- **Source:** AMLA — Regulatory instruments tracker (`T1-02`) — https://www.amla.europa.eu/policy/regulatory-instruments_en
- **Detected:** 2026-06-16T14:04:44Z
- **Type / playbook:** institutional / institutional

## What was flagged

The HTML body of AMLA's **Regulatory instruments** index changed (stable hash, not rotating
noise), with no extractable per-item title diff — hence a manual-review flag. The benchmark for
this index is a **final RTS submitted to the Commission / adopted as a Delegated Regulation in the
OJEU**; the auto-fetch confirmed none yet.

Confirmed against the live source (fetched 2026-06-16):

- Status categories on the page: **"Consultation open"**, **"Consultation closed"**, **"Final
  report published"**.
- Only two instruments are at **"Final report published"**: the RTS on inherent/residual risk
  profiles (**Art 40(2) AMLD6**) and the RTS on selection for direct supervision (**Art 12(7)
  AMLAR**) — both already recorded in the repo.
- The page references only **pre-existing EBA Delegated Regulations** (e.g. (EU) 2019/758,
  (EU) 2018/1108) that stay applicable under Art 54(5) AMLAR until superseded — **not** new AMLA
  Delegated Regulations. **No mandate has advanced to submitted-to-Commission / adopted /
  OJEU-published.**

## Assessment

**In-scope** (AMLA Level-2/3 pipeline, this repo's `file_id`) but **not substantive for this
index** — noise. The index change most plausibly mirrors the **Group-wide RTS consultation status
flip** (item 5, closed 15 Jun 2026) already flagged and owned by **T1-01 / issue #2**, surfacing on
this index as a "Consultation closed" relabel. The status-changing event this index exists to catch
(a final RTS appearing as a Commission Delegated Regulation in the OJ) **has not occurred**.

Everything the index currently shows is already in the repo:

- `docs/amla-pipeline.md:54-57` — both finalised RTS (Art 40(2) AMLD6, Art 12(7) AMLAR) recorded.
- `docs/amla-pipeline.md:59-63` — "Next benchmark" already names "publication of any final RTS in
  the OJ as a Commission Delegated Regulation".
- `TIMELINE.md:27` — 2025-12 row records both RTS finalised.
- `STATUS.md:36,39,61` — "No package delegated regulation adopted yet"; OJ publication named as the
  status-changing benchmark.

## Plan

**Recommendation: close issue #4 as not-relevant (noise / superseded by #2).** No repo edit driven
by this issue.

1. No edits to `STATUS.md`, `TIMELINE.md`, or `docs/amla-pipeline.md` — the only substantive,
   in-scope movement (Group-wide RTS closure) is the subject of issue #2 and must not be duplicated
   here (one issue per plan/PR).
2. This plan file is the only artifact. The downstream files listed in `affects` need no change.
3. When the real benchmark fires (a final RTS submitted to the Commission, or adopted as a
   Delegated Regulation in the OJEU), that will arrive as a fresh tracker hit and gets its own
   issue/plan — likely a `council-text`-scale cascade into the pipeline table + `what-changed`.

## Verification

- Live source re-read confirms statuses are only "Consultation open/closed" + two "Final report
  published" (both already in repo); zero new "Delegated Regulation" / OJEU items.
- Cross-checked against `docs/amla-pipeline.md:54-63`, `TIMELINE.md:27`, `STATUS.md:36,39,61` — all
  consistent; nothing to add.
- `python3 .claude/skills/transcribe-council-extract/linkcheck.py .` → "0 broken".
- Close the issue once the PR merges: `gh issue close 4 --comment "Not relevant: index change
  mirrors the consultation status flip tracked in #2; no mandate advanced to submitted/adopted/OJEU."`
